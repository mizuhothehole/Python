textbrfore="Hello"
keys="3rfcf3ertv4hvgvh5vehrtehvvvjthvrvthtrvhtyvj6yt4rfhevmym"
print textbrfore;

class App(object):

   def __init__(self):
      self
   def encryptionFunction(self,textContent,key):
      self
      code = ""
      for iterating_var in textContent:
         code+=(iterating_var+ key)
      return code
   def recryptionFunction(self,textContent,key):
      self
      code = ""
      code = textContent.replace(key,'')
      return code

app = App()
code = app. encryptionFunction(textbrfore, keys)
print code
recode = app. recryptionFunction(code, keys)
print recode